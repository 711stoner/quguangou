# 取关狗项目维护说明

最后更新：2026-09-22

这份文档用于下次继续修改时快速接上，不用再重新梳理项目结构、版本和文案。

## 重要链接

- 官网：<https://711stoner.github.io/quguangou/>
- GitHub 仓库：<https://github.com/711stoner/quguangou>
- 最新 Release：<https://github.com/711stoner/quguangou/releases/latest>
- Ko-fi 支持页：<https://ko-fi.com/wuhuwang>
- 反馈与留言评论区：<https://x.com/woyongyuangeson/status/2098974760801870134?s=20>

## 当前版本状态

- 当前浏览器版本：Chrome `v0.2.15`、Edge `v0.2.16`、Firefox `v0.2.15`。
- 当前商店构建包：`extension/dist/quguangou-chrome-v0.2.15.zip`、`extension/dist/quguangou-edge-v0.2.16.zip`、`extension/dist/quguangou-firefox-v0.2.15.zip`。
- Edge v0.2.16 的 SEO 标题为 `取关狗｜X（Twitter）取关名单与批量拉黑工具`；短名称仍为 `取关狗`。
- v0.2.15 起，Chrome / Edge / Firefox 安装包统一加入 `default_locale: zh_CN` 与 `_locales/zh_CN/messages.json`，默认简介为简体中文。
- 当前名单文件：`extension/data/blocklist.json`
- 当前名单版本：`2026.09.13.2`
- 当前名单数量：64 个账号
- 当前名单状态：格式有效、无重复账号

## 当前产品说明

取关狗不是个人取关检测器。它不会扫描用户的关注/粉丝历史，也不会告诉用户“刚刚谁取关了你”。项目整理整个 X 中文社区的互关后取关线索，并在用户确认后帮助用户依次拉黑名单账号。

核心定位：

> 不是查谁取关了你，而是整理社区取关记录，帮你一键处理名单中的账号。

扩展说明：

- 用户登录自己的 X 账号。
- 用户先查看在线社区名单；网络异常时自动使用上次缓存或扩展内置备用名单。
- 用户主动点击后，扩展依次打开名单账号主页并尝试拉黑。
- 不读取 X 密码。
- 不上传用户账号数据。
- 不依赖远程服务器完成拉黑。
- 非 X Corp. 官方产品，与 X Corp. 无隶属或认可关系。

## 执行节奏与风险提示

- 每批最多尝试处理 20 个账号。
- 一批结束后进入 30 分钟冷却期。
- 冷却期结束后，需要用户手动再次点击继续。
- 设计目标是降低短时间连续操作的风险，但不能保证账号安全。
- X 官方规则禁止非 API 自动化操作；使用可能导致账号受限、暂停或永久停用。
- 页面文案要提醒用户自行阅读平台规则、自行判断风险。

## 名单原则

名单线索主要来自三类来源：

1. X 中文社区公开帖子中对“互关后取关”的曝光记录。
2. 互关帖下主动留言、但主页关注与粉丝比例明显异常的账号线索，例如关注约 1000、粉丝约 3000。比例仅作为待核对线索，不单凭比例认定或收录。
3. 用户私信或公开投稿的“互关后又取关我”记录，并附截图、时间线等材料。

- 线索经人工整理和复核后再进入名单。
- 名单来自整个 X 中文社区，不局限于当前用户自己的关注列表。
- 被收录账号可以说明理由并提交补充材料，申请复核、更正或移除。
- 已经显示“已屏蔽”的账号也可以加入名单。

## 安装说明重点

官网应写清楚：

1. 下载 ZIP。
2. 双击解压。
3. 打开 Chrome 浏览器新标签页。
4. 复制 `chrome://extensions`，粘贴到浏览器最上方的地址栏，不要粘贴到搜索框。
5. 打开“开发者模式”。
6. 点击“加载已解压的扩展程序”。
7. 选择解压后的整个 `quguangou-chrome-v版本号` 文件夹，不要点里面的单个文件。

当前官网还需提示：

- Google Chrome 插件市场版本仍在上传中，请先手动下载 ZIP 使用。
- 从 v0.2.14 起，单纯增加、删除或纠正名单账号不需要重新发布插件。
- 插件打开时优先读取 GitHub Pages 在线名单；失败时依次回退到上次成功缓存和插件内置备用名单。
- 只有插件代码、权限、界面或运行逻辑变化时，才需要重新提交浏览器商店。

## Firefox 主页、赞助与收款配置

- Firefox Add-ons「主页」：`https://711stoner.github.io/quguangou/`
- Firefox Add-ons「捐款网址」：`https://ko-fi.com/wuhuwang`
- Mozilla 允许 `ko-fi.com` 作为捐款网址域名，因此商店后台直接填写完整 Ko-fi 页面地址。
- Ko-fi 已成功连接现有中国大陆 PayPal 个人账户；PayPal 页面已确认该账户可用于 Ko-fi 收款。
- 当前收款链路：`Firefox Add-ons / 官网 → Ko-fi → PayPal`。
- 当前中国大陆 PayPal 个人账户无法创建 PayPal.Me；Donation Page 也提示所在国家或地区不可用。
- PayPal「付款链接与按钮」要求 Business 账户；当前不走 Business 路线，避免额外企业主体/营业执照验证要求。
- Stripe 不是当前必需项；现阶段 Ko-fi + PayPal 已经打通，后续如增加 Stripe 再单独记录。

## 浏览器兼容说明

- Firefox：v0.2.13 已通过 Mozilla 审核。
- Google Chrome：官网当前仍提供 ZIP 手动安装包。
- 其他浏览器应使用各自对应的构建或商店版本，不要把 Chrome ZIP 直接当作 Firefox 安装包。

## 商单助手底部入口

官网底部、页脚上方放置一张横向宣传海报，用来引导用户前往“账号商业价值测试”。入口链接：

> https://jiedanfadan.com/value-check

## 下次更新名单流程

1. 从用户截图中提取 X 用户名，格式统一为不带 `@` 的 `username`，展示时再加 `@`。
2. 检查是否重复。
3. 优先确认截图里账号可识别、能打开主页；已屏蔽账号也可以加入。
4. 只有在互关帖中明确表示“互关、交友”的账号，关注 / 粉丝比例明显异常时才作为重点核对线索；比例本身不作为单独收录依据。
5. 修改唯一名单源 `extension/data/blocklist.json`。
6. 更新名单版本号，例如 `2026.09.22.1`。
7. 运行名单测试后提交并推送 GitHub。
8. 等 GitHub Pages 部署完成后，访问 `https://711stoner.github.io/quguangou/extension/data/blocklist.json` 确认新版本已上线。
9. **无需重新提交 Firefox / Edge / Chrome 商店。**

如果改的是插件代码、权限、界面或执行逻辑，则另走“插件版本发布”流程：提升 `manifest.json` / `package.json` 版本、运行全部测试、重新构建 ZIP，并分别提交各浏览器商店。

## GitHub 仓库介绍

仓库 About 已设置：

- Description：`取关狗：整理 X 中文社区公开曝光、互关帖线索与用户投稿形成的取关名单，并支持用户主动一键依次拉黑`
- Website：`https://711stoner.github.io/quguangou/`
- Topics：`chrome-extension`、`github-pages`、`privacy`、`x`、`blocklist`、`unfollow`

README 首页也应保持清楚，类似一个项目主页：标题、简介、官网、下载、功能、目录、反馈与风险提示。

## Chrome Web Store 上架与更新注意事项

### 开发者账户

- Chrome Web Store 开发者注册需要一次性支付 5 美元注册费。
- 发布/更新扩展的 Google 账号需要开启两步验证。
- 如果计划把插件开发作为长期业务、以后有收费插件或付费功能，即使大部分插件免费，也更适合声明为 Trader（交易者）。
- Trader 不要求必须有公司，个人也可以作为 Trader。
- Trader 验证会要求手机验证码、身份证明，以及能证明姓名和当前地址的材料。
- Payments Profile 的姓名、国家/地区和地址必须使用真实、可验证的信息；不要为了通过验证随便填写地址。
- 如果驾照本身显示当前地址，并且与 Payments Profile 中的地址一致，可以作为地址证明材料之一。

### Chrome 提交包

当前 Chrome 构建包：

`extension/dist/quguangou-chrome-v0.2.15.zip`

注意：

- Chrome 只上传 `quguangou-chrome-v版本号.zip`，不要上传 Edge 或 Firefox 包。
- 不要上传 macOS 自动生成的 `._quguangou-chrome-v版本号.zip` 隐藏辅助文件。
- ZIP 根目录必须直接包含 `manifest.json`。
- 当前扩展使用 Manifest V3。

### “包裹上的标题”和“软件包摘要”为什么不能修改

Chrome Web Store 后台中的：

- “包裹上的标题”来自 `manifest.json` 的 `name`。
- “软件包摘要”来自 `manifest.json` 的 `description`。

这两个字段由扩展包决定，上传 ZIP 后在商店后台会被锁定，不能直接编辑。

如果需要修改标题或摘要：

1. 修改 `extension/manifest.json` 中的 `name` 或 `description`。
2. 提高扩展版本号，例如 `0.2.14 → 0.2.15`。
3. 同步更新 `package.json` 和浏览器对应 manifest。
4. 运行测试并重新构建。
5. 生成新的 `quguangou-chrome-v0.2.15.zip`。
6. 在 Chrome Web Store 后台上传新版软件包并重新提交审核。
7. 新版本审核并发布后，Chrome Web Store 上的标题/摘要会随新包更新。

可以先用当前版本上线，之后再通过更高版本修改标题、摘要、功能或权限，不必为了文案问题重新创建商品。

### 当前 Chrome 商店推荐资料

- 商品语言：中文（简体）
- 类别：社交网络 / Social Networking
- 首页：`https://711stoner.github.io/quguangou/`
- 支持：`https://github.com/711stoner/quguangou/issues`
- 商店图标：使用 `extension/assets/icon-128.png`
- 全球宣传视频：没有可先留空
- 官方网站字段：只有完成 Google Search Console 网站所有权验证后再选；否则可暂时留空

### Chrome 商店截图规格

- 至少上传 1 张。
- 最多 5 张。
- 尺寸只能使用 `1280 × 800` 或 `640 × 400`。
- 文件格式：JPEG 或 24 位 PNG。
- PNG 不能带透明背景。
- 截图应展示扩展真实功能，不要宣传扩展不存在的能力。
- 商店公开截图中如展示名单，优先对具体 X 用户名做脱敏，避免把具体账号当作宣传素材公开指认。

### 当前文案原则

商店描述必须与实际功能一致：

- 不写成“自动检测谁取关了你”。
- 明确说明它不是个人取关检测器。
- 强调名单来自 X 中文社区公开线索、用户投稿和人工整理。
- 强调所有拉黑操作由用户主动确认后执行。
- 不声称名单中的账号已被系统自动判定为“单方面取关”。
- 隐私说明、商店文案、README 和插件真实权限保持一致。
